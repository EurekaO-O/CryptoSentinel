# CryptoSentinel 环境变量配置示例
# 复制此文件为 .env 并填入真实值

# Telegram 配置（必须设置）
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# 数据源配置（可选）
AHR999_URL=https://dncapi.bostonkey.com/api/v2/index/ahr999

# 定时任务配置（可选）
# 格式: 秒 分 时 日 月 周
# 默认: 每周一上午9点
CRON_SPEC=0 0 9 * * 1

# 杠杆率（可选，默认1.0）
LEVERAGE=1.0

# 启动时是否立即执行一次（可选，默认false）
RUN_ON_START=false

# 时区（可选，默认Asia/Shanghai）
TZ=Asia/Shanghai
